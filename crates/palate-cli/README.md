# palate-cli

Command-line package for Palate file type detection.

This package owns the `palate` executable and depends on the core `palate` library for detection results.

```sh
cargo install --path crates/palate-cli
palate path/to/file-or-directory
```
