#!/usr/bin/env bash

#MISE description="Build the application (Release)"
#MISE sources=["Cargo.toml", "crates/**/*.rs"]
#MISE outputs=["target/release/palate"]
#MISE depends=["test"]

echo "🚀 Starting Frontend release build..."
proj_dir=$(git rev-parse --show-toplevel)
cd "$proj_dir" || exit 1

cargo build --release
